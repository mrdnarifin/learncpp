// We really should have a header guard here, but will omit if for simplicity ( we'll cover header guards in the next lesson)

// Thi is the content of the .h file, which where the declarations go
int add(int x, int y); // function prototype for add.h -- dont forget the semicolon;

